% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/univariate.R
\name{univariate}
\alias{univariate}
\title{Univariate response curve plot}
\usage{
univariate(
  model,
  x = NULL,
  predict_data = NULL,
  fun = predict,
  ...,
  n = 100,
  ylab = "Prediction",
  rug = TRUE,
  ylim = NULL,
  color = "deepskyblue2",
  nrows = NULL,
  ncols = NULL
)
}
\arguments{
\item{model}{A fitted model object that supports prediction.}

\item{x}{A data frame or raster containing predictor variables. If `predict_data` is provided, this argument is ignored.}

\item{predict_data}{A data frame containing values at which predictions should be made. If `NULL`, `x` must be provided.}

\item{fun}{A function used to generate predictions from the model. Defaults to `predict`.}

\item{...}{Additional arguments passed to `fun`.}

\item{n}{Integer, number of points to sample for each predictor variable (default: 100).}

\item{ylab}{Character, label for the y-axis (default: "Prediction").}

\item{rug}{Logical, whether to include a rug plot along the x-axis (default: `TRUE`).}

\item{ylim}{Numeric vector of length 2, specifying the limits of the y-axis. If `NULL`, limits are automatically set.}

\item{color}{Character, colour of the response curve (default: "orangered2").}

\item{nrows}{Integer, number of rows in the plot grid. If `NULL`, it is automatically determined.}

\item{ncols}{Integer, number of columns in the plot grid. If `NULL`, it is automatically determined.}
}
\value{
A `ggplot2` object containing the response curves arranged in a grid.
}
\description{
This function generates response curves for a given model by varying one predictor at a time while keeping others constant.
}
\examples{
# Example usage with a fitted model
library(randomForest)
data(iris)
model <- randomForest(Sepal.Length ~ ., data = iris[, 1:4])
response_plot <- univariate(model, x = iris[, 2:4])
print(response_plot)
}
