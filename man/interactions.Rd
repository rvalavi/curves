% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interactions.R
\name{interactions}
\alias{interactions}
\title{Rank pairwise interactions using second-order ALE}
\usage{
interactions(
  model,
  x = NULL,
  predict_data = NULL,
  pairs = NULL,
  fun = NULL,
  ...,
  n = 10,
  response = NULL,
  details = FALSE
)
}
\arguments{
\item{model}{A fitted model object that supports prediction.}

\item{x}{A data frame or raster containing predictor variables. If
\code{predict_data} is provided, this argument is ignored.}

\item{predict_data}{A data frame containing values at which predictions
should be made. If \code{NULL}, \code{x} must be provided.}

\item{pairs}{Optional specification of predictor pairs to rank. Supply
\code{NULL} to rank all unique pairs, a character or numeric vector of length 2
for a single pair, or a list/data frame/matrix of pairs. Numeric pairs are
interpreted as predictor column indices. Non-numeric pairs are ignored
with a warning because second-order ALE currently supports numeric
predictor pairs only.}

\item{fun}{A function used to generate predictions from the model. If
\code{NULL}, the generic \code{predict()} is used.}

\item{...}{Additional arguments passed to \code{fun}.}

\item{n}{Integer, maximum number of ALE intervals per numeric predictor
(default: \code{10}).}

\item{response}{Optional column name or index to select when \code{fun} returns
multiple predictions per row. If \code{NULL} and exactly two prediction columns
are returned, the second column is used.}

\item{details}{Logical; if \code{FALSE} (default), return the ranking data frame.
If \code{TRUE}, return a list containing the ranking plus the ALE tables and
pair specifications used internally by \code{\link[=bivariate]{bivariate()}} when \code{top_n} filters
ALE surfaces. The returned tables keep interpolated values for unsupported
cells so callers such as \code{\link[=bivariate]{bivariate()}} can decide whether to mask or show
them.}
}
\value{
If \code{details = FALSE}, a data frame ordered from highest to lowest
interaction strength, with columns \code{rank}, \code{pair}, \code{predictor_1},
\code{predictor_2}, \code{strength}, \code{support}, \code{supported_cells}, and
\code{total_cells}. If \code{details = TRUE}, a list with components \code{ranking},
\code{pair_specs}, and \code{tables}.
}
\description{
Quantify and rank pairwise interaction strength using the same centred
second-order accumulated local effects (ALE) surfaces used by
\code{\link[=bivariate]{bivariate()}} with \code{method = "ale"}. This is also the ranking used
internally by \code{bivariate(method = "ale", top_n = ...)} when it selects the
strongest surfaces to plot.
}
\details{
Let \eqn{\hat{f}_{ab,ALE}(c_k, d_l)} denote the centred second-order ALE
surface for predictor pair \eqn{(a, b)} evaluated at cell centre
\eqn{(c_k, d_l)}, and let \eqn{n_{k,l}} be the number of observed rows in
that ALE cell. The returned interaction strength is the count-weighted
root-mean-square ALE magnitude,
\deqn{I_{ab} =
  \sqrt{\frac{\sum_{k,l} n_{k,l}\hat{f}_{ab,ALE}(c_k, d_l)^2}
               {\sum_{k,l} n_{k,l}}}.}
This uses the observed ALE cell counts as weights, so densely supported
parts of the predictor space contribute more than sparse cells. Additive
predictor pairs score zero, and larger values indicate stronger non-additive
joint effects. Cells with zero observations are excluded from the score.
}
\examples{
data(iris)
model <- lm(
  Sepal.Length ~ Sepal.Width * Petal.Length + Petal.Width,
  data = iris
)

interactions(
  model,
  x = iris[, c("Sepal.Width", "Petal.Length", "Petal.Width")]
)
}
\references{
Molnar, C. (2025). \emph{Interpretable Machine Learning: A Guide for Making Black
Box Models Explainable} (3rd ed.). \url{https://christophm.github.io/interpretable-ml-book/}

Apley, D. W., & Zhu, J. (2020). Visualizing the effects of predictor
variables in black box supervised learning models. \emph{Journal of the Royal
Statistical Society: Series B}, 82(4), 1059-1086.
}
