% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bivariate.R
\name{bivariate}
\alias{bivariate}
\title{Bivariate response surface plot}
\usage{
bivariate(
  model,
  x = NULL,
  predict_data = NULL,
  pairs = NULL,
  fun = stats::predict,
  ...,
  n = 40,
  plot_type = c("heatmap", "contour", "surface"),
  zlab = "Prediction",
  bins = 8,
  palette = grDevices::hcl.colors(12, "YlOrRd", rev = TRUE),
  nrows = NULL,
  ncols = NULL
)
}
\arguments{
\item{model}{A fitted model object that supports prediction.}

\item{x}{A data frame or raster containing predictor variables. If
\code{predict_data} is provided, this argument is ignored.}

\item{predict_data}{A data frame containing values at which predictions
should be made. If \code{NULL}, \code{x} must be provided.}

\item{pairs}{Optional specification of predictor pairs to plot. Supply
\code{NULL} to plot all unique pairs, a character or numeric vector of length 2
for a single pair, or a list/data frame/matrix of pairs. Numeric pairs are
interpreted as predictor column indices.}

\item{fun}{A function used to generate predictions from the model. Defaults
to \code{predict}.}

\item{...}{Additional arguments passed to \code{fun}.}

\item{n}{Integer, number of points to sample for each numeric predictor
variable (default: 40).}

\item{plot_type}{Character, plot type. Use \code{"heatmap"} for a static surface,
\code{"contour"} for filled contours, or \code{"surface"} for an interactive 3D
surface. The 3D surface requires the suggested \code{plotly} package and a
single numeric predictor pair.}

\item{zlab}{Character, label for the response legend or z-axis (default:
\code{"Prediction"}).}

\item{bins}{Integer, number of contour bins for \code{"heatmap"} overlays and
\code{"contour"} plots.}

\item{palette}{Character vector of colours used for the response scale.}

\item{nrows}{Integer, number of rows in the plot grid. If \code{NULL}, it is
automatically determined.}

\item{ncols}{Integer, number of columns in the plot grid. If \code{NULL}, it is
automatically determined.}
}
\value{
A \code{ggplot2} object for static plot types or a \code{plotly} widget for
\code{plot_type = "surface"}.
}
\description{
This function generates bivariate response plots for a given model by
varying two predictors at a time while keeping others constant.
}
\examples{
data(iris)
model <- lm(
  Sepal.Length ~ Sepal.Width + Petal.Length + Petal.Width,
  data = iris
)
response_plot <- bivariate(
  model,
  x = iris[, c("Sepal.Width", "Petal.Length", "Petal.Width")]
)
print(response_plot)

if (requireNamespace("plotly", quietly = TRUE)) {
  surface_plot <- bivariate(
    model,
    x = iris[, c("Sepal.Width", "Petal.Length", "Petal.Width")],
    pairs = c("Sepal.Width", "Petal.Length"),
    plot_type = "surface"
  )
  surface_plot
}
}
