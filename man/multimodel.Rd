% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multimodel.R
\name{multimodel}
\alias{multimodel}
\title{Creating a multimodel response curve plot}
\usage{
multimodel(
  models,
  x = NULL,
  predict_data = NULL,
  fun = stats::predict,
  ...,
  n = 100,
  ylab = "Prediction",
  se = TRUE,
  rug = TRUE,
  ylim = NULL,
  color = "deepskyblue2",
  se_color = "grey80",
  nrows = NULL,
  ncols = NULL
)
}
\arguments{
\item{models}{A list object of fitted models that support prediction.}

\item{x}{A data frame or raster containing predictor variables. If
\code{predict_data} is provided, this argument is ignored.}

\item{predict_data}{A data frame containing values at which predictions
should be made. If \code{NULL}, \code{x} must be provided.}

\item{fun}{A function used to generate predictions from the model. Defaults
to \code{predict}.}

\item{...}{Additional arguments passed to \code{fun}.}

\item{n}{Integer, number of points to sample for each numeric predictor
variable (default: 100).}

\item{ylab}{Character, label for the y-axis (default: \code{"Prediction"}).}

\item{se}{Standard deviation ribbon for the averaged curve.}

\item{rug}{Logical, whether to include a rug plot along the x-axis (default:
\code{TRUE}).}

\item{ylim}{Numeric vector of length 2, specifying the limits of the y-axis.
If \code{NULL}, limits are automatically set.}

\item{color}{Character, colour of the response curve (default:
\code{"deepskyblue2"}).}

\item{se_color}{Fill colour of the standard deviation ribbon.}

\item{nrows}{Integer, number of rows in the plot grid. If \code{NULL}, it is
automatically determined.}

\item{ncols}{Integer, number of columns in the plot grid. If \code{NULL}, it is
automatically determined.}
}
\value{
A \code{ggplot2} object containing the response curves arranged in a grid.
}
\description{
This function generates response curves for several models by varying one
predictor at a time while keeping others constant.
}
\examples{
data(iris)
models <- list(
  lm(Sepal.Length ~ Sepal.Width + Petal.Length, data = iris),
  lm(Sepal.Length ~ Petal.Width + Petal.Length, data = iris)
)
response_plot <- multimodel(
  models,
  x = iris[, c("Sepal.Width", "Petal.Length", "Petal.Width")]
)
print(response_plot)
}
